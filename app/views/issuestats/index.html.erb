<h1>Issue Statistics</h1>

<table>
<%= form_for :issuestats, url: project_issuestats_show_path(@project) do |f| %>
    <tr><p><td><%= f.label "Start date between: " %></td><td> <%= f.text_field :start_date, :type => "date", :required => true %> and  <%= f.text_field :end_date, :type => "date", :required => true %> </td></p></tr>
    <tr><p><td><%= f.label "Intervention status: " %></td><td> <%= select :issuestats, :intervention_status, options_for_select(@options), {:include_blank => 'Select One'}, {:multiple => false, :required => true} %></td></p></tr>
    <tr><p><td><%= f.label "Intervention time (min): " %></td><td> <%= f.text_field :intervention_time, :type => "number", :required => true %></td></p></tr>
    <tr><p><td><%= f.label "Resolve status: " %></td><td> <%= select :issuestats, :resolve_status, options_for_select(@options), {:include_blank => 'Select One'}, {:multiple => false, :required => true} %></td></p></tr>
    <tr><p><td><%= f.label "Resolve time (min): " %></td><td> <%= f.text_field :resolve_time, :type => "number", :required => true %></td></p></tr>
    <tr><td><%= f.submit 'Show statistics' %></td><td></td></tr>
<% end %>
</table>

<p>
  Notes:
  <br>
  Intervention time: Time between <b>first 'intervention status'</b> change and <b>'start date'</b> as minutes.
  <br>
  Resolve time: Time between <b>last 'resolve status'</b> change and <b>first 'intervention status'</b> as minutes.
</p>
